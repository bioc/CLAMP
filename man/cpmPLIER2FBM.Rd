% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cpmPLIER2FBM}
\alias{cpmPLIER2FBM}
\title{Compute CPM on a file-backed matrix for PLIER2 (in-place)}
\usage{
cpmPLIER2FBM(fbm_counts, block_size = 1000, ncores = 1)
}
\arguments{
\item{fbm_counts}{A bigstatsr::FBM of raw counts (genes x samples).}

\item{block_size}{Integer; columns per block (default 1000).}

\item{ncores}{Integer; number of cores to use for parallel operations (default 1).}
}
\value{
Invisibly returns the modified FBM (now holding CPM values).
}
\description{
Compute CPM on a file-backed matrix for PLIER2 (in-place)
}
\examples{
library(bigstatsr)
mat <- matrix(c(10, 20, 30, 40, 50, 60),
    nrow = 2,
    dimnames = list(c('gene1', 'gene2'), paste0('sample', seq_len(3)))
)
fbm <- FBM(nrow(mat), ncol(mat), init = mat)
cpmPLIER2FBM(fbm, block_size = 1)
}
