% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cleanFBM}
\alias{cleanFBM}
\title{Clean a Filebacked Big Matrix (FBM) by log-transforming and handling NAs}
\usage{
cleanFBM(fbm, ncores = 1, log2_transform = TRUE)
}
\arguments{
\item{fbm}{A \code{bigmemory::FBM} or \code{bigstatsr::FBM} object.}

\item{ncores}{Integer; number of cores to use for parallel operations
(default 1).}

\item{log2_transform}{Logical; enable automatic \code{log2(x + 1)} transformation
when the maximum value is at least 100 (default TRUE). Set to FALSE to
skip transformation. Missing values are still replaced with zero.}
}
\value{
A list with:
\describe{
\item{max_value}{The maximum value encountered in the FBM (after log
transformation if applied).}
\item{had_na}{Logical indicating whether any NA values were found
and filled.}
}
}
\description{
This function inspects an FBM to determine if log-transformation is needed
(based on value range) and whether NA values are present. If the maximum
value is >= 100 and \code{log2_transform = TRUE}, it applies a log2(x + 1)
transformation in-place. If any
NA values are detected, they are replaced with 0.
}
\details{
Modifies the FBM in place. Uses \code{bigstatsr::big_apply()} to process in
parallel-safe chunks.
}
\examples{
fbm <- bigstatsr::FBM(3, 4, init = matrix(
    c(0, 1, 2, NA, 100, 200, 300, 400, 5, 6, 7, 8),
    nrow = 3
))
cleanFBM(fbm, ncores = 1)

}
