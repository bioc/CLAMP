% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{allAgainstAllAUCs}
\alias{allAgainstAllAUCs}
\title{Compute all-vs-all AUC matrix}
\usage{
allAgainstAllAUCs(B, target)
}
\arguments{
\item{B}{A numeric matrix of predictions (samples × features).}

\item{target}{A binary matrix of the same number of rows as \code{B}
(samples × targets), where 1 indicates positive and 0 indicates negative.}
}
\value{
A numeric matrix of AUC values (features × targets).
}
\description{
Calculates the area under the ROC curve (AUC) for all pairs of columns
between a prediction matrix \code{B} and binary targets in \code{target}.
Each column of \code{B} is ranked, and AUC is computed based on how well
the ranks separate positive vs. negative samples in each target column.
}
\examples{
set.seed(1)
B <- matrix(rnorm(100), nrow = 20)
target <- matrix(sample(0:1, 40, replace = TRUE), nrow = 20)
allAgainstAllAUCs(B, target)

}
